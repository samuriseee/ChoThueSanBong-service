import swaggerJsdoc from 'swagger-jsdoc';
import { env } from './env';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Soccer Rental Service API',
      version: '1.0.0',
      description: 'API documentation for the Soccer Rental Service',
    },
    servers: [
      {
        url: `http://localhost:${env.port}`,
        description: 'Development server',
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },
  // Look for swagger comments in these files
  apis: ['./src/api/**/*.ts', './src/api/**/*.route.ts'],
};

export const swaggerSpec = swaggerJsdoc(options);
